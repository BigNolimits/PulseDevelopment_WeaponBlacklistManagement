Config = {}

-- Default blacklisted weapons (spawn names)
Config.BlacklistedWeapons = {
}

-- Check interval (ms)
Config.CheckInterval = 1000

-- Enable ox_lib notifications
Config.UseOXLib = true
